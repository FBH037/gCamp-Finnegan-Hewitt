%html
  %h1.page-header About
  .row
  .col-md-4
    = image_tag("reading.jpg", class: "img-responsive")
    %p gCamp is extrememly active. Check out our killer stats below!
    = "#{pluralize(Project.all.count, "Project")}, #{ pluralize(Task.all.count, "Task")}, #{pluralize(Membership.all.count, "Project Member")}, #{pluralize(User.all.count, "User")}, and #{pluralize(Comment.all.count, "Comment")}"

  .col-md-8
    %p We are dedicated to the makers, the creators, the risk-takers.
    %p We wake up in the middle of the night and furiously write down ideas about how to make your online life better.
    %p Human collaboration and communication is what drives us, and it infuses every feature of gCamp.
  .row
